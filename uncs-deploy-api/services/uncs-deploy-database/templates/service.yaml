apiVersion: v1
kind: Service
metadata:
  name: {{ include "uuncs-deploy-database.fullname" . }}
  labels:
    {{- include "uuncs-deploy-database.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "uuncs-deploy-database.selectorLabels" . | nindent 4 }}
